{目标格式} 开头的文档是我想要的 dofile 格式。主要特点是：
1. 列宽不超过 76，否则采用 /// 换行；
   - 换行时缩进 4 个空格
   - 功能相似的选项可以放在同一行，否则要独占一行
2. 同一个功能的代码作为一个代码块，合理使用空行

{待处理} 开头的文档是需要处理的，主要问题：
- 单行代码太长
- 空行太多或太少，导致结构不合理

你的任务：帮我写一组提示词，用以将各类不符合规范的 dofile 转换成与 {目标格式} 风格和格式相近的 dofile。

